registry=https://registry.npmjs.org/
# Override the @cloudflare scope to the public registry. Without this, users
# on machines with a ~/.npmrc that routes @cloudflare to an internal registry
# (common at Cloudflare itself) will fail to install @cloudflare/* packages.
@cloudflare:registry=https://registry.npmjs.org/
